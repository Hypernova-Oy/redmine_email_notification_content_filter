<p><%= l(:mail_body_reminder, :count => link_to(@issues.size, @reminder_issues_url), :days => @days).html_safe %></p>

<ul>
<% @issues.each do |issue| -%>
  <li><%= link_to_issue(issue, :project => false, :only_path => false, :subject => false) %> (<%= due_date_distance_in_words(issue.due_date) %>)</li>
<% end -%>
</ul>
